# F1 컨셉 SVG 에셋 권고 문서

**PITWALL 렌더 레이어 저수준 도형 대체 계획**

작성일: 2026-07-31 · 대상: src/render/trackRenderer.ts · 범위: SVG 프리미티브 9개 요소

---

## 1. 에셋 소스 평가

GitHub API로 검증된 9개 소스를 라이선스, 적합성, 추출 방법으로 평가했다. 모든 추천 에셋은 이 표에서만 선택한다.

| 소스 | 라이선스 | 별점 | 상태 | 추출 방법 | 비고 |
|---|---|---|---|---|---|
| Templarian/MaterialDesign | Apache-2.0 | 11,285★ | ✅ | `svg/flag-checkered.svg` 직접 복사 | 체커드 플래그, 가우스 기반, 고품질 |
| tabler/tabler-icons | MIT | 21,263★ | ✅ | `icons/outline/*.svg`, car/car-suv/gauge/tool 검색 | 500+ 이상의 탈것 아이콘 풀, 가장 큰 카탈로그 |
| game-icons/icons | CC BY 3.0 | 1,325★ | ✅ | `icons/delapouite/checkered-flag.svg`, `icons/lorc/checkered-diamond.svg` | 저작자 표시 필수 (HTML에 주석), 게임 스타일 가능 |
| oracle/font-apex | 오픈소스 (Oracle APEX) | 112★ | ✅ | `svgs/small/flag-checkered.svg`, `svgs/large/flag-checkered.svg` | 엔터프라이즈 수준 완성도, 크기 옵션 |
| Tencent/tdesign-icons | MIT | 85★ | ✅ | `svg/formula.svg` (F1 스타일 수식 기호) | 중국 신뢰도 높음, F1 직결 아이콘 드물음 |
| FortAwesome/Font-Awesome | CC BY 4.0 (무료세트) | 76,790★ | ✅ | `svg/` 디렉터리, flag-checkered/gauge 등 | 웹 폰트가 아닌 SVG 추출 방식으로만 (라이선스 유지) |
| lucide-icons/lucide | ISC | 23,717★ | ✅ | `icons/car.svg`, `icons/gauge.svg`, `icons/fuel.svg` | 심플하고 일관된 스타일, 24×24 뷰박스 표준 |
| **WarmBed/PITWALL** | **Apache-2.0** | **10★** | ⚠️ 부적합 | PNG 스크린샷만 존재 | **SVG 소스 없음** — 자산 소스로 사용 불가 |
| bloop16/ioBroker.f1 | MIT | 1★ | ⚠️ 낮음 | `admin/f1.svg` (F1 로고 풍) | 품질 낮음, 최후의 수단 |

**권고 우선순위 (사용 순서):**
1. Templarian/MaterialDesign (체커드 플래그, 기본 아이콘) — 가장 광범위하고 안정적
2. tabler/tabler-icons (자동차류, 연료, 속도) — 비즈니스용 높은 완성도
3. game-icons/icons (경주 용어, 독특한 스타일) — F1 게임 감성에 부합
4. oracle/font-apex (엔터프라이즈 표준 아이콘) — 신뢰도 높음
5. lucide-icons/lucide (미니멀 아이콘) — 가벼운 파일 크기

---

## 2. PITWALL 렌더 요소 → 추천 에셋 매핑표

쉐이프 카탈로그의 9개 요소. 각 행은 현재 프리미티브, 역할, 대체 에셋, 난이도, 리스크를 나열한다.

### 2.1 요소 1: Track Centerline (경로)

| 속성 | 값 |
|---|---|
| **현재 프리미티브** | `<path>` (polyline, 폐곡선) |
| **파일 위치** | src/render/trackRenderer.ts:177, drawCenterline() |
| **역할** | 경주 트랙 중앙선 (시뮬레이션 베이스, 시각적 참조) |
| **현재 구현** | track.points 배열로부터 생성된 폐곡선 (최대 320점), 색상 #2a323d, 스트로크 51 |
| **대체 에셋 후보** | 실서킷 벡터 일러스트레이션 (Monaco, Silverstone, Spa 등) |
| **추천 소스** | game-icons (circuit-style) 또는 직접 제작 (실서킷 벡터) |
| **라이선스** | game-icons: CC BY 3.0 (저작자 표시) / 직제: 별도 |
| **난이도** | **높음** — 실서킷마다 다른 도형 필요 |
| **리스크** | **높음** — 파일 크기 증가, 개인정보 노출 위험 (실제 좌표 유출) |
| **구현 방식** | 현재 procedural 경로 유지 권고. 실서킷 일러스트는 v2 이후 고려 |
| **프라이버시 체크** | ✅ 통과 — 개인 식별 정보 없음 |

**결론:** 프리미티브 유지. 에셋 교체는 파일 크기 증가 + 개인정보 위험 때문에 미연기.

---

### 2.2 요소 2: Pit Lane Path (경로)

| 속성 | 값 |
|---|---|
| **현재 프리미티브** | `<path>` (polyline, 개방 끝) |
| **파일 위치** | src/render/trackRenderer.ts:196, drawPitLane() |
| **역할** | 피트레인 진입/진출 복도 (피트박스 위치 마킹) |
| **현재 구현** | pit box 좌표 배열로부터 생성, 색상 #1c222b, 스트로크 40.8 |
| **대체 에셋 후보** | 스타일화된 피트레인 복도 (corner markers, pit crew icons) |
| **추천 소스** | tabler/tabler-icons: tool.svg + game-icons (pit-mark 계열) |
| **라이선스** | tabler: MIT / game-icons: CC BY 3.0 |
| **난이도** | **중간** — 박스 마킹용 아이콘 추가만 필요 |
| **리스크** | **낮음** — 개인정보 없음, 파일 크기 미미 |
| **구현 방식** | 현재 경로 유지 + 피트박스 좌표에 작은 도구/플래그 아이콘 오버레이 |
| **프라이버시 체크** | ✅ 통과 |

**결론:** 선택적. 피트 박스 corner markers 추가로 시각 명확도 증대 가능.

---

### 2.3 요소 3: Pit Lane Label ("PIT" 텍스트)

| 속성 | 값 |
|---|---|
| **현재 프리미티브** | `<text>` (텍스트) |
| **파일 위치** | src/render/trackRenderer.ts:212, drawPitLane() |
| **역할** | 피트레인 꼬리 위치에 "PIT" 라벨 표시 |
| **현재 구현** | textContent: 'PIT', 동적 위치, CSS 스타일 적용 |
| **하드 룰 위반** | **§6.3 트랙 위 텍스트 금지** — 명시적 위반 |
| **대체 에셋 (필수)** | **체커드 플래그** (PIT 진입/진출 신호) |
| **추천 소스** | Templarian/MaterialDesign: flag-checkered.svg (최우선) |
| **라이선스** | Templarian: Apache-2.0 |
| **난이도** | **낮음** — 텍스트 요소를 SVG `<image>` 또는 인라인 `<svg>` 로 1회 교체 |
| **리스크** | **낮음** — 파일 크기 증가 미미 (아이콘 하나) |
| **구현 방식** | SVG `<text>` 제거 → `<image>` 또는 인라인 SVG 삽입 (크기 20×20 viewBox 추천) |
| **우선순위** | **P0 (긴급)** — 하드 룰 준수 필수 |
| **프라이버시 체크** | ✅ 통과 (텍스트 제거로 더 개선) |

**결론:** **즉시 교체 필수.** "PIT" 텍스트를 체커드 플래그 아이콘으로 대체하여 하드 룰 준수.

---

### 2.4 요소 4: Car Body (Hot Slot) — 글리프

| 속성 | 값 |
|---|---|
| **현재 프리미티브** | `<path>` (3가지 형상) |
| **파일 위치** | src/render/trackRenderer.ts:231, hotSlot() |
| **역할** | 활동 중 차량 신체 표시 (클래스별 색상 + 형상) |
| **현재 구현** | glyphPath(shape: 'circle' / 'triangle' / 'square') → SVG path d 속성, 크기 10 units (GLYPH_SIZE=5) |
| **색상 인코딩** | **색 + 형상 이중 인코딩 (하드 룰 준수)** |
| | H (Hypercar): 빨강 (#ff5c5c) + 삼각형 |
| | P (Prototype): 파랑 (#4dc3ff) + 원 |
| | GT (GT3): 노랑 (#ffd24d) + 사각형 |
| **대체 에셋 후보** | **F1 스타일 자동차 실루엣** (클래스별) |
| | H: LMH 하이퍼카 프로필 (뾰족한 각도) |
| | P: LMP2 스포츠카 프로필 (유선형) |
| | GT: GT3 와이드 스탠스 (광폭) |
| **추천 소스** | Templarian/MaterialDesign: car-sports.svg (H) / tabler/tabler-icons: car.svg (P), car-suv.svg (GT) |
| **라이선스** | Templarian: Apache-2.0 / tabler: MIT |
| **난이도** | **중간** — 3가지 형상 에셋 준비 필요 |
| **리스크** | **중간** — 파일 크기 증가 가능, 색상 인코딩 유지 필수 |
| **구현 방식** | glyphPath() 함수의 path d 문자열을 에셋-기반 SVG로 교체. 각 클래스마다 다른 `<image>` href. CSS fill 속성으로 색상 유지 |
| **제약사항** | **색 + 형상 이중 인코딩 보존 필수** — 에셋 교체 후에도 circle/triangle/square 구분 가능해야 함. 색상만으로는 부족 |
| **프라이버시 체크** | ✅ 통과 — 클래스만 인코딩, 개인 식별 정보 없음 |

**결론:** **우선순위 높음 (P1).** 세 가지 자동차 실루엣으로 교체하되, 색 + 형상 이중 인코딩 반드시 유지.

---

### 2.5 요소 5: Fuel Ring (원형)

| 속성 | 값 |
|---|---|
| **현재 프리미티브** | `<circle>` (채우지 않은 링) |
| **파일 위치** | src/render/trackRenderer.ts:232, hotSlot() |
| **역할** | 활동 중 차량 상태 표시 (연료/활력 게이지 고리) |
| **현재 구현** | `<circle r="8">`, 스트로크 색상 클래스별, 채우기 없음 |
| **대체 에셋 후보** | 연료 게이지 호 (Bezier path) 또는 피트스톱 아이콘 |
| **추천 소스** | lucide-icons: fuel.svg, gauge.svg 또는 tabler: gauge.svg |
| **라이선스** | lucide: ISC / tabler: MIT |
| **난이도** | **중간** — 현재 원형 유지하되 선택적으로 스타일 개선 |
| **리스크** | **낮음** — 파일 크기 거의 증가 없음 |
| **구현 방식** | 옵션 A: 현재 원형 유지. 옵션 B: Bezier 호로 전환하여 연료 % 시각화 (stroke-dasharray 사용). 옵션 C: 팀 라디오 우선순위 링으로 재해석 |
| **프라이버시 체크** | ✅ 통과 |

**결론:** 선택적. 현재 원형 유지 권장 (최소 변경). v2에서 연료 % 게이지로 진화 고려.

---

### 2.6 요소 6: Alert Mark (정지 사유 표시) — 경로

| 속성 | 값 |
|---|---|
| **현재 프리미티브** | `<path>` (hand-drawn 선) |
| **파일 위치** | src/render/trackRenderer.ts:238, hotSlot() |
| **역할** | 차량 정지 사유 시각화 (에러 vs 한도) |
| **현재 구현** | STOP_MARK 상수 (lines 51-62): |
| | error: "!" 느낌표 (수직선 + 점), 빨강 #ff5c5c |
| | limit: "=" 게이지 (가로선 2개), 노랑 #ffd24d |
| **대체 에셋 후보** | **아이콘화** (프로퍼 심볼로) |
| | error: 정지 표지판 (stop sign) 또는 빨간 X |
| | limit: 연료캔 (fuel can) 또는 노랑 깃발 (yellow flag) |
| **추천 소스** | tabler/tabler-icons: alert-circle.svg, alert-triangle.svg, flag-2.svg 등 |
| **라이선스** | tabler: MIT |
| **난이도** | **낮음** — 2개 아이콘만 필요 |
| **리스크** | **낮음** |
| **구현 방식** | STOP_MARK 상수의 path d 문자열을 에셋 SVG 경로로 교체. 색상 (빨강/노랑)은 유지 |
| **우선순위** | **P1** — 하드 룰 준수 (텍스트 아닌 형상으로 구분) 유지 + 가독성 향상 |
| **프라이버시 체크** | ✅ 통과 — 기계적 상태만 인코딩 |

**결론:** **권고 높음 (P1).** 현재 hand-drawn 선을 정지 표지판 + 연료캔 아이콘으로 교체하여 명확성 증대.

---

### 2.7 요소 7: Car Label (차번 텍스트) — Hot Slot

| 속성 | 값 |
|---|---|
| **현재 프리미티브** | `<text>` (텍스트) |
| **파일 위치** | src/render/trackRenderer.ts:22, makeLabel() + 245, hotSlot() |
| **역할** | 차량 번호 라벨 (조건부: LABEL_MAX_CARS=8까지만) |
| **현재 구현** | textContent: car.carNumber (3자리, 예: "042"), y offset 24 |
| **하드 룰 위반** | **§6.3 트랙 위 텍스트 금지** — 명시적 위반 |
| **대체 에셋 (필수)** | **숫자판 배지** (클래스별 색상) 또는 **제거** |
| **추천 소스** | Templarian/MaterialDesign 또는 tabler/tabler-icons (배지 스타일 아이콘) — 구체 파일명은 구현 시 선정 |
| **라이선스** | Templarian: Apache-2.0 / tabler: MIT |
| **난이도** | **중간** — 텍스트 → 숫자판 배지 SVG 교체 또는 완전 제거 |
| **리스크** | **중간** — 밀도 높은 트랙에서 배지 겹침 가능성 |
| **구현 방식** | 옵션 A: 텍스트 제거 (밀도 규칙 LABEL_MAX_CARS 강화). 옵션 B: 글리프 근처 작은 숫자 배지 (SVG `<foreignObject>` 또는 인라인 HTML) |
| **우선순위** | **P0 (긴급)** — 하드 룰 준수 필수 |
| **프라이버시 체크** | ⚠️ 부분 위반 — car_id는 노출하지 않지만, carNumber 표시는 트랙 위 텍스트 금지 |

**결론:** **즉시 제거 권고.** 만약 유지한다면 작은 배지로 축소하되, 밀도 > 8인 경우는 동작 중단 (현재 규칙 강화).

---

### 2.8 요소 8: Car Body (Cold Slot) — 글리프

| 속성 | 값 |
|---|---|
| **현재 프리미티브** | `<path>` (hot과 동일한 3가지 형상) |
| **파일 위치** | src/render/trackRenderer.ts:266, coldSlot() |
| **역할** | 비활동 차량 신체 표시 (색 + 형상) |
| **현재 구현** | glyphPath() 동일, 크기 10 units (hot과 동일) |
| **색상 인코딩** | **색 + 형상 이중 인코딩** (hot과 동일) |
| **대체 에셋 후보** | **hot car와 동일한 F1 실루엣**, 선택적으로 회색 또는 투명도 감소 변형 |
| **추천 소스** | tabler/tabler-icons (hot과 동일) |
| **라이선스** | tabler: MIT |
| **난이도** | **낮음** — hot car 에셋 재사용, CSS opacity 조정만 필요 |
| **리스크** | **낮음** — 변경 최소화 가능 |
| **구현 방식** | hot car와 동일한 에셋 사용. CSS 클래스 `cold-slot`에서 opacity: 0.6 등으로 차별화. 또는 `filter: grayscale(50%)` 추가 |
| **프라이버시 체크** | ✅ 통과 |

**결론:** **우선순위 낮음.** hot car 에셋 정의 후 자동으로 상속 가능.

---

### 2.9 요소 9: Car Label (차번 텍스트) — Cold Slot

| 속성 | 값 |
|---|---|
| **현재 프리미티브** | `<text>` (텍스트) |
| **파일 위치** | src/render/trackRenderer.ts:22, makeLabel() + 267, coldSlot() |
| **역할** | 비활동 차량 번호 라벨 (조건부 표시) |
| **현재 구현** | hot car label과 동일 |
| **하드 룰 위반** | **§6.3 트랙 위 텍스트 금지** — 명시적 위반 (hot과 동일) |
| **대체 에셋 (필수)** | **제거** 또는 **배지** |
| **난이도** | **낮음** — hot car label과 함께 일괄 처리 가능 |
| **우선순위** | **P0 (긴급)** — hot car label과 동시 처리 |

**결론:** hot car label (요소 7)과 함께 제거 또는 배지 변환.

---

## 3. 하드 룰 준수 체크리스트

PITWALL PRD §6 (프라이버시 가드레일)과 v1 MVP 결정에 따라, 이 에셋 교체가 만족해야 할 9개 항목.

### 3.1 색상 + 형상 이중 인코딩 보존

- [ ] 자동차 글리프 (hot + cold): 삼각형/원/사각형 형상 구분 유지
- [ ] 각 형상은 **반드시** 특정 클래스(H/P/GT)에 일대일 대응
- [ ] 색상 변경만으로는 충분하지 않음 — 색맹 사용자도 구분 가능해야 함
- [ ] 에셋 교체 후에도 CSS `fill` 속성으로 색상 상속 가능해야 함

**검증:** trackRenderer.ts line 207-214 CLASS_STYLE 매핑 유지 + 새 에셋 3개 (triangle/circle/square 대응)

---

### 3.2 트랙 위 텍스트 라벨 금지

- [ ] "PIT" 텍스트 → **체커드 플래그 아이콘으로 대체 (필수)**
- [ ] Car label (차번) → **제거 또는 밀도 기반 배지로 축소 (필수)**
- [ ] 남은 텍스트 요소는 `<text>` 아닌 `<tspan>` 또는 SVG 대체 이미지만 허용

**검증:** trackRenderer.ts line 212 (pit label), line 22/245/267 (car labels) 제거 또는 대체 확인

---

### 3.3 개인 식별 정보 DOM 노출 금지

- [ ] `car_id` 속성이 DOM 요소의 data- 또는 class에 노출되지 않음
- [ ] car.carNumber도 가능하면 제거 (또는 시각적 배지에만, 텍스트 금지)
- [ ] 에셋 SVG 파일 내에 개인 정보 포함 금지 (사람 이름, 팀 로고 등)

**검증:** grep "carId" src/render/trackRenderer.ts — 모두 data 속성으로만 (렌더에 영향 없음)

---

### 3.4 제로 의존성 유지

- [ ] 모든 추천 에셋은 **SVG 문자열로 인라인** (외부 로드 금지)
- [ ] 번들 싱글 파일 buildSingleFile.ts에 에셋 경로 추가 시 모두 base64 인코딩 또는 문자열 삽입
- [ ] `<image xlink:href>` 사용 금지 (HTTP 요청 유발)

**검증:** npm run build:single 후 dist/pitwall.html 크기 < 50 KB 유지

---

### 3.5 파일 크기 예산

현재 빌드 크기: **24.7 kB JS**

- [ ] 추가 에셋 인라인 시 예산 증가 상한: **+15 KB** (최대 40 KB)
- [ ] 각 에셋 파일 크기 개별 체크:
  | 에셋 | 권고 크기 | 비고 |
  |---|---|---|
  | 자동차 실루엣 3개 (H/P/GT) | < 3 KB 합계 | SVG path 경량화 (Potrace 또는 SimplifyJS) |
  | 체커드 플래그 (PIT) | < 0.5 KB | 단순 도형 |
  | 정지 표지판 (error) | < 0.5 KB | 단순 도형 |
  | 연료캔 (limit) | < 0.5 KB | 단순 도형 |
  | 여유분 | 10 KB | 미래 에셋 추가 용 |

**검증:** npm run build:single 실행 후 파일 크기 로그 확인

---

### 3.6 F1 상표 리스크 회피

- [ ] "F1" 문자 직접 표기 금지 (상표 침해)
- [ ] 대신 "race telemetry", "endurance race", "pit strategy" 등 일반 언어 사용
- [ ] 에셋 중 "Formula 1" 로고 명시 아이콘 사용 금지
- [ ] Tencent tdesign-icons의 formula.svg 사용 시: "Formula"로만 표기 (F1은 안 됨)

**권고:** 상표 고지 문구를 HTML 주석 또는 README에 추가:
```html
<!-- 이 애플리케이션은 F1과 무관합니다. LiteLLM 사용량을 8시간 내구 레이스 은유로 표현합니다. -->
```

**검증:** 최종 HTML 에서 "F1" 검색 (logo, trademark 제외)

---

### 3.7 라이선스 명시

- [ ] 모든 에셋마다 라이선스 명기 필수
- [ ] 인라인 SVG 코멘트로 출처 + 라이선스 표기:
```svg
<!-- Asset: flag-checkered | Source: Templarian/MaterialDesign | License: Apache-2.0 -->
```
- [ ] HTML 주석 섹션에 전체 라이선스 요약

**검증:** src/render/trackRenderer.ts 내 모든 인라인 SVG에 위 형식 적용

---

### 3.8 암비언트 철학 준수

- [ ] 정보 밀도 증가 금지 — 에셋은 현재 형상을 단순히 대체만 할 것 (새 정보 추가 금지)
- [ ] 애니메이션 금지 (로딩 느낌, 깜빡임 등) — 정적 SVG만
- [ ] 색상 과포화 금지 — 3가지 클래스 색 (빨강/파랑/노랑) 유지

**검증:** 렌더 결과 스크린샷 vs 현재 v1 비교 — 시각적 정보량 동일 확인

---

### 3.9 DOM 구조 최소 변경

- [ ] trackRenderer.ts의 createElementNS 호출 부분만 변경 (다른 파일 수정 금지)
- [ ] CSS 클래스, 애트리뷰트명 변경 금지
- [ ] 테스트 코드 (trackRenderer.test.ts)는 자동으로 통과해야 함 (변경 금지)

**검증:** git diff src/ 후 trackRenderer.ts만 수정 확인

---

## 4. 구현 우선순위

낮은 노력 × 높은 임팩트 순서로 정렬. 각 항목은 하드 룰 준수 상태, 파일 크기, 사용자 경험 개선도를 고려했다.

### P0 (긴급 — 하드 룰 준수)

#### P0-1: "PIT" 텍스트 → 체커드 플래그 아이콘 대체
- **요소:** 요소 3 (Pit Lane Label)
- **현재 상태:** 트랙 위 텍스트 (하드 룰 §6.3 위반)
- **작업:** trackRenderer.ts:212 `<text>` 제거 → Templarian flag-checkered.svg 인라인
- **파일 크기 영향:** +0.3 KB
- **난이도:** 낮음 (1시간)
- **이유:** 명시적 하드 룰 위반 — 법적 리스크. 가장 간단한 수정.

#### P0-2: 차량 번호 라벨 제거 또는 배지 축소
- **요소:** 요소 7, 9 (Car Labels hot + cold)
- **현재 상태:** 트랙 위 텍스트 (하드 룰 §6.3 위반)
- **작업:** trackRenderer.ts:22, 245, 267 `<text>` 제거. 또는 밀도 기반 조건부 배지로 축소 (LABEL_MAX_CARS 강화)
- **파일 크기 영향:** -0.2 KB (제거 시)
- **난이도:** 낮음 (1시간)
- **이유:** 법적 준수. 사용자 경험에는 긍정적 (텍스트 겹침 제거).

---

### P1 (높음 — 시각 개선 + 프라이버시 강화)

#### P1-1: 자동차 글리프 → F1 스타일 실루엣 대체 (3개 클래스)
- **요소:** 요소 4, 8 (Car Bodies hot + cold)
- **현재 상태:** 원/삼각형/사각형 프리미티브 (기능하지만 단순함)
- **작업:** glyphPath() 함수의 path d 문자열을 에셋으로 교체. 3개 에셋 필요:
  - H (Hypercar): Templarian/MaterialDesign car-sports.svg
  - P (Prototype): tabler/tabler-icons car.svg (표준)
  - GT (GT3): tabler/tabler-icons car-suv.svg (광폭)
- **파일 크기 영향:** +2 KB (3개 경량화 에셋)
- **난이도:** 중간 (2-3시간) — 3개 에셋 테스트 필요
- **이유:** 시각적 임팩트 큼 (가독성 향상). 색+형상 이중 인코딩 유지. F1 테마 강화.

#### P1-2: 정지 표시 → 아이콘화 (error/limit 구분)
- **요소:** 요소 6 (Alert Mark)
- **현재 상태:** hand-drawn 선 (에러 vs 한도 구분 불명확)
- **작업:** STOP_MARK 경로 d 속성을 아이콘으로 교체:
  - error: tabler alert-circle.svg 또는 alert-triangle.svg
  - limit: lucide fuel.svg 또는 tabler alert-triangle.svg
- **파일 크기 영향:** +0.6 KB
- **난이도:** 낮음 (1시간)
- **이유:** 명확성 증대. 하드 룰 준수 유지 (텍스트 아닌 도형).

---

### P2 (선택적 — 품질 향상)

#### P2-1: Fuel Ring → 게이지 호 스타일 (선택)
- **요소:** 요소 5 (Fuel Ring)
- **현재 상태:** 단순 원형 링
- **작업:** 선택사항 — 현재 유지 또는 Bezier 호로 진화 (v2)
- **파일 크기 영향:** 0 KB (현재 유지) / +1 KB (호 변환)
- **난이도:** 높음 (SVG path 조작) — v2 이후 권고
- **이유:** 낮은 우선순위. 현재 구현으로 충분함.

#### P2-2: Pit Lane Path → 코너 마킹 추가 (선택)
- **요소:** 요소 2 (Pit Lane Path)
- **현재 상태:** 단순 경로
- **작업:** 피트박스 좌표에 작은 도구/플래그 아이콘 오버레이
- **파일 크기 영향:** +0.5 KB
- **난이도:** 중간 (2시간)
- **이유:** 시각적 명확성. 필수는 아님.

---

### 실행 순서 (권고)

1. **P0-1 + P0-2 (동시)**: 텍스트 라벨 제거/교체 (1일, 2시간)
2. **P1-1 (우선)**: 자동차 실루엣 에셋 통합 (1-2일, 3시간)
3. **P1-2**: 정지 표시 아이콘화 (0.5일, 1시간)
4. **전체 테스트 + 번들 크기 검증** (1일, 2시간)
5. **P2-* (선택)**: v2 백로그로 이동

**총 예상 시간:** 3-4일 (개발 + 테스트)

---

## 5. 라이선스 요약

| 에셋 소스 | 라이선스 | 요구사항 | 구현 방법 |
|---|---|---|---|
| Templarian/MaterialDesign | Apache-2.0 | 라이선스 복사본 포함 | HTML/src 루트 LICENSE 파일 추가 |
| tabler/tabler-icons | MIT | 라이선스 + 저작권 표시 | SVG 주석 + HTML 하단 크레딧 |
| game-icons/icons | CC BY 3.0 | 저작자 표시 필수 | 각 아이콘 SVG에 저작자명 기재 + HTML 크레딧 |
| lucide-icons/lucide | ISC | 라이선스 표시 | HTML 크레딧 섹션에 포함 |
| oracle/font-apex | 오픈소스 (Oracle) | 라이선스 표시 | HTML 크레딧 섹션에 포함 |
| FortAwesome/Font-Awesome | CC BY 4.0 | 저작자 표시 필수 | HTML 크레딧 섹션에 포함 |

**권고 크레딧 섹션 (HTML 하단에 추가):**

```html
<!-- Asset Credits -->
<!-- SVG Icons: Templarian/MaterialDesign (Apache-2.0), tabler/tabler-icons (MIT), 
     game-icons/icons (CC BY 3.0 - Delapouite, Lorc), 
     lucide-icons/lucide (ISC), Oracle APEX (open source) -->
<!-- Full license texts in src/assets/LICENSES/ -->
```

---

## 6. 마이그레이션 체크리스트

최종 검증 단계. 구현 후 모든 항목 확인 필수.

### 코드 레벨

- [ ] trackRenderer.ts 수정 완료 (자동차 글리프, alert marks, text labels)
- [ ] 인라인 SVG에 라이선스 주석 추가
- [ ] trackRenderer.test.ts 통과 (변경 없음)
- [ ] TypeScript 컴파일 오류 0개 (tsc)

### 시각 검증

- [ ] 브라우저에서 3가지 클래스 글리프 색+형상 구분 가능 확인
- [ ] "PIT" 아이콘이 명확하게 표시되는지 확인
- [ ] 100대 차량 밀도에서 텍스트 겹침 없음 확인 (차번 제거 후)
- [ ] hot/cold 차체 시각 차별화 유지 확인 (CSS opacity/filter)

### 번들 및 성능

- [ ] npm run build:single 성공 (단일 HTML 파일 생성)
- [ ] dist/pitwall.html 크기 < 40 KB 확인
- [ ] 렌더 성능 동일 (100대 차량 60fps 유지)
- [ ] 단일 파일 `file://` 로딩 정상 확인 (CORS 없음)

### 하드 룰 재확인

- [ ] 트랙 위 텍스트 0개 (grep "createElementNS.*text")
- [ ] car_id DOM 노출 0개 (grep "data-car-id" 제외 data 속성)
- [ ] 색상 + 형상 이중 인코딩 유지 (H/P/GT 각 3가지 테스트)

### 라이선스 & 법적

- [ ] 모든 에셋 출처 명시 (주석)
- [ ] 라이선스 준수 확인 (CC BY 저작자 표시 등)
- [ ] "F1" 상표 문제 없음 (검색 결과 0)
- [ ] README.md에 에셋 출처 업데이트

---

## 참고: 현재 상태 vs 제안 비교

### 현재 (v1)

| 요소 | 프리미티브 | 라벨 | 비고 |
|---|---|---|---|
| 트랙 | path | 없음 | ✅ |
| 피트레인 | path | "PIT" 텍스트 | ⚠️ 하드 룰 위반 |
| Hot car | path (3형) | carNumber 텍스트 | ⚠️ 하드 룰 위반 |
| 연료 링 | circle | 없음 | ✅ |
| 정지 표시 | path (선) | 없음 | ✅ (형상 인코딩) |
| Cold car | path (3형) | carNumber 텍스트 | ⚠️ 하드 룰 위반 |

### 제안 (v1.1)

| 요소 | 프리미티브 | 라벨 | 비고 |
|---|---|---|---|
| 트랙 | path | 없음 | ✅ 유지 |
| 피트레인 | path | 체커드 플래그 아이콘 | ✅ 준수 |
| Hot car | SVG 에셋 (3가지) | 없음 (또는 배지) | ✅ 준수 + 시각 개선 |
| 연료 링 | circle | 없음 | ✅ 유지 |
| 정지 표시 | 아이콘 (정지/연료) | 없음 | ✅ 준수 + 명확성 |
| Cold car | SVG 에셋 (3가지) | 없음 (또는 배지) | ✅ 준수 + 시각 개선 |

---

## 결론

PITWALL 렌더 레이어의 9개 SVG 요소 중:

- **즉시 교체 (P0):** 요소 3, 7, 9 — 하드 룰 §6.3 트랙 위 텍스트 제거 → 아이콘 또는 제거
- **높은 우선순위 (P1):** 요소 4, 8 — 자동차 글리프 → F1 스타일 실루엣 (색+형상 이중 인코딩 유지)
- **중간 우선순위 (P1):** 요소 6 — 정지 표시 → 명확한 아이콘 (error/limit 구분)
- **선택적 (P2):** 요소 2, 5 — 품질 향상 (v2 이후)
- **유지 (현 상태):** 요소 1 — 트랙 경로 프리미티브 유지

**법적 리스크:** 현재 2개 문제 (P0-1, P0-2) 즉시 해소 필수.
**파일 크기:** 추가 예산 +3.5 KB (24.7 → 28 KB) — 상한 40 KB 내.
**개발 기간:** 3-4일 (개발 + 테스트).

모든 추천 에셋은 GitHub에서 검증된 오픈소스 소스에서만 선택했으며, 라이선스 준수 및 프라이버시 하드 룰 (색+형상 이중 인코딩, 개인 정보 노출 금지)을 만족한다.
