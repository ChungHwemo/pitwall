# Stack
- TypeScript, Vite, Vitest, jsdom, vite-node.
- `package.json`: runtime dependency 0; scripts include `npm test`, `npm run build`, `npm run build:single`.
- UI has no framework; DOM/SVG is constructed directly.