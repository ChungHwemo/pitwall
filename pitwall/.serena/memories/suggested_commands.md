# Commands
- From `pitwall/`: `npm test` runs Vitest once.
- `npm run build` runs the Vite production build.
- `npm run build:single` builds and bundles one offline HTML file.
- `npm run dev` starts the Vite dev server.