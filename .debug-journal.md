# Debug Journal — track cars move forward and backward
Started: 2026-08-03
Goal: Determine why PITWALL track cars visibly move forward and backward erratically.

## Environment snapshot
- Runtime: TypeScript/Vite WebView plus macOS bundled app
- Entry: `cd pitwall && npm run dev` or `open pitwall/dist/PITWALL.app`
- Git state: uncommitted Grok fix in four files; preserve it; no commit/push during diagnosis
- References read: node.md, playwright-cli.md, methodology/00-setup.md, 02-investigate.md, 04-oracle-triple.md

## Hypotheses
1. [OPEN] Projection applies a signed progress delta and an anchor update can move a car backward; distinguishing evidence: sampled progress/visual position before and after each anchor, `projection.ts` delta logic; fix projection monotonicity.
2. [OPEN] Multiple transform writers or CSS animation conflict with renderer translation; distinguishing evidence: computed transform/style writer count and DOM attributes/classes; fix transform ownership.
3. [REFUTED] Circuit coordinate orientation or wrap-around makes increasing normalized progress map to decreasing screen position on a segment; distinguishing evidence: circuit point samples and monotonicity across segment boundaries; fix coordinate mapping.

## Failed hypothesis round counter
- Round 1: H2/H3 refuted; H1 confirmed by browser retrace evidence.

## Artifacts to revert
- [ ] `.debug-journal.md` — remove after diagnosis/fix cleanup.
- [ ] Temporary dev server/browser processes — stop after QA.
- [ ] Temporary repro scripts/screenshots — remove after evidence capture.

## Findings
<!-- Append verbatim runtime/code observations here. -->

### 2026-08-03 — projection/browser motion evidence
- Source: `pitwall/dist/h3-evidence/FINDINGS.md`, real Chrome trace from demo-large at 100x speed.
- Value: one car produced `171 reversals / 318 frames`, worst direction cosine `-1.0`; raw coordinates retraced `(430.70,424.15)→(419.00,423.70)→(407.85,429.96)→(397.92,437.66)` and then the exact reverse sequence.
- Interpretation: `positionAt` cannot create bit-identical reverse traversal from increasing progress; input `visual` progress oscillated upstream.
- Refutes/Confirms: H3 REFUTED; H1 CONFIRMED. Likely seam is `projection.ts:Projector.step()` pull-back/lead-clamp.

## Root cause
`Projector.step()` accepted a backward target as the current anchor. The resulting lead clamp repeatedly cancelled the positive projected velocity, so the same visual position was returned after a backward correction. The fix keeps the last forward anchor whenever `shortest(anchor, target) <= 0`.

### Red phase
- Test: `tests/projection.test.ts` / `큰 후퇴 보정 뒤에도 같은 위치의 화면 주행은 계속 전진한다`
- Observed: `expected 0.20400000000000001 to be greater than 0.20400000000000001`

### Green phase
- Targeted projection suite: 18 tests passed.

### Final validation
- `cd pitwall && npm run build`: passed.
- `cd pitwall && npm test`: 48 files and 854 tests passed.
- Adversarial regression sequence: backward correction followed by repeated forward/equal anchors produced strictly advancing projection values; no new projection test failures.

## Artifacts to retain for this fix
- `pitwall/tests/projection.test.ts` — deterministic regression coverage for repeated anchor corrections.
- `pitwall/src/render/projection.ts` — minimal state fix.
- `REVIEW.md` — review item status/evidence update.

---

# Debug Session — choppy track motion and unreadable animation
Started: 2026-08-08T23:30:00+09:00
Goal: Reproduce and remove frame-to-frame choppiness, then use racing broadcast/game benchmarks to redesign the track for immediate readability.

## Environment snapshot (Phase 0)
- Runtime: Node/Vite TypeScript browser UI, Vitest/jsdom tests, production Chromium surface; native wrapper remains a later lane.
- Entry: `cd pitwall && npm run preview -- --host 127.0.0.1 --port 4173` after `npm run build:single`.
- Git HEAD at session start: Task 3 worker is still finalizing on top of `80e2e54e9af586f4ce3bfba1019075bcf3cf6bc0`; do not investigate a moving tree until that worker commits.
- References read: debugging `SKILL.md`, `runtimes/node.md`, `tools/playwright-cli.md`, `methodology/00-setup.md`, `methodology/02-investigate.md`; Playwright CLI skill; ultimate-browsing skill and insane-search README.

## Hypotheses
1. [OPEN] Projection step size is frame-count-based instead of elapsed-time-based, so uneven rAF cadence creates visibly unequal motion — distinguishing evidence: browser frame timestamps versus transform/path deltas and `Projector.step()` calculations — if true, fix is: time-scale interpolation.
2. [OPEN] Sparse/bursty source anchors exhaust the bounded lead and force long holds followed by catch-up jumps — distinguishing evidence: event/anchor timestamps, lead distance, velocity, and repeated transform plateaus correlated in one trace — if true, fix is: cadence-aware easing.
3. [OPEN] Hot/cold membership or renderer pooling changes which physical glyph is updated, producing apparent teleportation even if individual projector values are smooth — distinguishing evidence: stable `data-car-id`, node identity, hot/cold membership, and transform continuity in the same trace — if true, fix is: stable visual ownership.

## Failed hypothesis round counter
- New session Round 1: pending after Task 3 freezes.

## Artifacts to revert
- [ ] `/tmp/pitwall-racing-benchmark/` — downloaded benchmark imagery/manual extracts. Remove: `rm -rf /tmp/pitwall-racing-benchmark`.
- [ ] Motion trace/repro scripts under `/tmp/pitwall-motion-*`. Remove: `rm -rf /tmp/pitwall-motion-*`.
- [ ] Preview server/browser sessions created for motion debugging. Stop/close and prove ports free.
- [ ] Any temporary instrumentation requested by investigators. Revert before the TDD fix.

## Findings — new session
- User runtime observation: `이동이 뚝뚝 끊기잖아, 자연스럽게 이동해야 해`.
- User visual verdict: current track UI/UX animation readability is near zero; requested F1/NASCAR/Cyber Formula/racing-game benchmarking and explicitly allowed Three.js.
- Insane-search probes succeeded for official F1 TV, NASCAR Race Center, Cyber Formula SIN DREI, and SIN VIER manual URLs.
- Official/reference visual search shows a common pattern: full-track spatial overview, one selected subject with high-contrast telemetry, literal timing/status text, restrained marker colors, and stable context; Cyber Formula adds a focused speed/boost HUD instead of making every object equally loud.

### Diagnosis round 1 — 2026-08-09
- H1 REFUTED at `9b38c57083f3c9dc7fc5f7102219617b4820f7a9`: rAF timestamps flow through `PitwallApp.frame` to `Projector.step`, which advances from elapsed `dt`, not invocation count. Evidence: `.omo/evidence/ulw/review-ui-realtime-20260808/G001-review-md-ui-ux/a1/motion-h1-clock.md`.
- H2 mechanism reproduced twice: after anchors `0.20@1000` and `0.30@2000`, 500 16ms samples produced 63 moving and 437 identical frames; the projector reached `anchor + lastStep = 0.40` and then held for about 7 seconds. Evidence: `motion-h2-anchor.md`.
- H3 mechanism traced: hot/cold SVG groups are pooled by array index while interpolation is keyed by `carId`; ranking or membership reorder reassigns a physical node to another car and writes its distant transform in one render. Evidence: `motion-h3-identity.md`.
- Root-cause status remains PENDING toggle proof until the failing-first tests fail on the current mechanisms, pass with the minimal implementation, and fail again when the implementation is reverted.

---

# Debug Session — stale car label popup
Started: 2026-08-09
Goal: Trace every remaining source and UI surface that can display `로컬카탄` and fix only a confirmed app-content path.

## Environment snapshot
- Runtime: TypeScript/Vite browser app, Vitest/jsdom tests.
- Entry: existing `http://127.0.0.1:5173` dev server.
- Working tree: pre-existing track/broadcast changes preserved.
- References read: debugging setup/investigation/fix/QA/cleanup, Node runtime, Playwright CLI.

## Hypotheses
1. [REFUTED] Current page or clicked car/detail DOM still contains the label — evidence: Playwright visible/body/HTML counts stayed zero across tower, detail, and all 14 car-hit clicks; fix: none.
2. [REFUTED] Browser chrome/address UI owns the label — evidence: no browser chrome was exposed; app DOM is the only inspected surface and current app URL/title contain no label; fix: none.
3. [CONFIRMED SOURCE RISK] A renderer can receive a raw custom-name map and bypass the shared sanitizer — evidence: `radioRenderer.ts:67-70` directly trims `names[msg.carId]`, while `carDisplayName` is the shared fallback seam; fix: centralize sanitization.

## Artifacts to revert
- [ ] This journal entry — retain only as session evidence, remove before completion if no source fix is made.
- [ ] Any temporary browser capture — browser state only; no repo artifact intentionally created.

## Findings
- Playwright after reload: `body=0`, `html=0`, `badVisible=0`; localStorage contained only `pitwall.sessions` and later `pitwall.dataset`, no `pitwall.carNames`.
- Tower click and all visible `.car-hit` clicks: popup/detail `.detail[data-selected="true"]` rendered, with `badBody=0` and `badVisible=0` for every click.
- Exact source literal search: only sanitizer rule in `pitwall/src/config/carNames.ts` and its regression fixture in `pitwall/tests/carNames.test.ts`; no UI source literal.
- `saveCarNames` persists raw maps, and `RadioRenderer.render` bypasses `carDisplayName`; both are concrete remaining source paths for a raw map supplied in-memory or by a future caller.
