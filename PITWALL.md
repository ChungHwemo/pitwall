# PITWALL — 현재 상태 전부

2026-07-31 기준 · 테스트 547개 통과 · tsc 클린 · 런타임 의존성 0

조직의 LLM 사용을 내구 레이스로 그리는 상시 노출 화면. 대시보드가 아니라
두 번째 모니터에 띄워 두고 **곁눈질로 읽는** 물건이다.

---

## 1. 실행

```bash
npm test                      # 547개
npm run dev                   # 웹 (시뮬레이터)
npm run fetch:limits          # 벤더 한도 갱신 → fixtures/limits.json
npm run import:real           # 로컬 로그 → fixtures/events.real.jsonl
npm run make:demo:all         # 더미 3벌 생성 (계정 4 / 14 / 40)
npm run build:real            # 데이터셋 전부 심은 단일 HTML
npm run build:app:real        # macOS 앱
npm run live:check            # 실시간 경로를 실계정 로그로 검증
open dist/PITWALL.app         # ⌘T 항상 위 · ⌘F 전체 화면
```

앱은 Xcode 없이 `swiftc` + `WKWebView`. ad-hoc 서명이라 **이 기기에서만** 돈다.

---

## 2. 데이터 — 화면에서 고른다

빌드 하나에 네 벌을 심고 상단 바에서 고른다. 고른 것은 기억되고,
**지어낸 데이터면 화면이 밝힌다** (`지어낸 데이터` 배지).

| 선택 | 내용 |
|---|---|
| **실시간** | 지금 이 기기에서 나가는 호출. 네이티브 앱에서만 |
| **실기록** | 이 기기 로그로 하루 재생 (계정 2, 2,097건) |
| 데모 · 소규모 | 지어냄 — 계정 4, 9,579건 |
| 데모 · 중규모 | 지어냄 — 계정 14, 29,735건, 모델 13종 |
| 데모 · 대규모 | 지어냄 — 계정 40, 78,076건, 모델 21종 |

데이터셋마다 최근 5,000건만 심는다(전부 넣으면 번들이 수십 MB). 잘린 양은 빌드 로그에 남는다.

더미는 실측 분포로 만든다 — 작업 토큰 중앙 1,950 · 캐시 비중 99.4% ·
호출 간격 중앙 5.2초 / p99 371초. 계정마다 성향이 다르다
(`heavy` / `steady` / `bursty` / `light` / `idle`). 단가는 카탈로그에서
**verified인 모델만** 쓴다.

### 오늘 실측 (2026-07-30)

| | |
|---|---|
| 호출 | 2,097건 · 계정 2 · 창 07–22시 (활동에서 파생) |
| 작업 토큰 | 11,315,653 |
| 캐시 재전송 | 737,148,747 (**전체의 98.4%**) |
| 비용 | $425.13 |
| **캐시가 아낀 돈** | **$3,265.41** (정가였다면 $3,690.54) |
| 소진 속도 | 6,462 work토큰/분 · $18.02/시간 |
| 에러 | 0건 |

---

## 3. 실시간

```
Swift LogTail (2초 폴링)          WebView
~/.claude/projects  ─┐
~/.codex            ─┼→ 새 줄만 → window.pitwallIngest(vendor, lines[])
~/.grok             ─┘                    ↓
                                    LiveSource → 기존 파서
```

- **파싱은 Swift가 안 한다.** 파서를 두 벌 두면 두 화면이 다른 사실을 말한다
- 처음 보는 파일은 **끝에 표시만** 한다 — 앱을 켤 때 며칠치가 쏟아지지 않게
- 계정 식별자는 `accountUuid` / `account_id`만 읽는다. 이메일·토큰은 안 읽는다
- 화면 도달 전 해시. `live:check`가 원문 유출 없음을 검사한다

한도는 로그에 없어(Claude) 빌드 시점 스냅샷을 쓰고 **판독 나이를 표시**한다.

---

## 4. 세 개의 축 — 절대 섞지 않는다

| 축 | 의미 | 단위 |
|---|---|---|
| **랩** | 한 바퀴 | 작업 토큰 **50,000** |
| **레이스** | 하루 | 활동에서 파생한 창 |
| **한도** | 벤더가 거는 벽 | 5시간 / 7일 롤링 |

**연료**(비용 예산)는 또 다른 것이고 한도가 아니다. 돈이 남아도 한도에 막히고,
돈이 없어도 호출은 나간다.

한도 소스가 없는 차는 한도에 걸렸다고 **주장하지 않는다**.

---

## 5. 화면

```
┌ HUD ─ 날짜·시각 · 경과/총 · $총액 · $/시간 · tok/분 · 페이즈 · [데이터] [범례] [설정]
├ 타워 (척추) ─ 계정당 한 줄
│   ▌12  claude-opus-5              RUN
│       ▁▂▅█▆▃  62% · 5시간창 · 리셋 2시간      $61.17
│                                     $19.0/시간 · 4.9k tok/분
├ 합계 ─ 계정 2 · 451콜 · 캐시 99% · $61.49 · $3265.41 아낌
├ 모델별 오늘 ─ claude-opus-5 ████ 417콜 · 949.3k · $61.17
└ 무전 ─ 상태 변화만
                        (우) 트랙 + 선택한 계정의 호출 피드
```

### 타워 한 줄 (F1 타이밍 타워 대응)

| F1 | PITWALL |
|---|---|
| POS 순위 | **없음** — 카넘버 고정순 (PRIV-2) |
| 팀색 막대 | 모델 등급색 |
| DRV 3글자 | 모델명 |
| TYRE | 한도 게이지 + 창 + 리셋 카운트다운 |
| GAP | $/시간 · tok/분 (**최근 30분 창**) |
| 섹터 블록 | 최근 30분 스파크라인 |
| 리타이어 흐림 | RUN / IDLE / PIT·LIM / PIT·ERR |

줄이 모자라면 **누구를 남길지**는 디렉터가 급한 순으로 고르되, 보이는 순서는
카넘버 그대로다. 10대 넘으면 한 단으로 접는다.

### 트랙

- 좌표계 가로비 1.5, 코스 둘레 약 3,900 (5로브)
- 주행선 폭 51, 레인 ±14, 지터 ±7, 글리프 반지름 5
- **자기간섭 검사** — 리본이 자기와 붙는 코스는 시드를 버린다
- **호 길이 등간격** — 진행률 0.5가 코스 절반을 뜻한다
- **매 실행 새 코스** (`?seed=`로 고정 가능)
- 정지 차량은 **인필드 피트**로. 에러(빨강 느낌표) vs 한도(호박색 게이지) 구분
- 9대 미만이면 카넘버 라벨
- 유휴 차량은 **남되 흐리게** — 지우면 트랙이 대부분 빈다

### 움직임

- **위치는 누적**(작업 토큰), **속도는 밝기**(발열)
- 발열은 로지스틱 — 500 tok/분 24% · 1,500 50% · 3,000 68% · 40,000 97%
- 샘플 사이는 속도 추정으로 이어 달린다 (앵커 + 전방투영 + 보간)
- 앞서기 한계는 **직전 한 걸음**, 프레임당 이동은 랩의 0.4%까지

---

## 6. 배율

```css
font-size: calc(var(--pw-zoom) * clamp(13px, min(100vw / 90, 100vh / 56.25), 34px));
```

1440×900에서 정확히 16px. 두 축을 다 보므로 낮고 넓은 창에서도 줄이 안 넘친다.
`--pw-zoom`은 시야거리 노브(책상 앞 0.85, 방 건너 1.3).

| 뷰포트 | root | 카넘버 | 무전 |
|---|---|---|---|
| 900×700 | 13px | 24.4 | 11.7 |
| 1600×1000 | 17.8 | 33.3 | 16.0 |
| 3840×2160 | 34.0 | **63.8** | **30.6** |

**SVG 안에는 rem을 쓰지 않는다** — viewBox가 이미 한 번 스케일한다.

---

## 7. 개인정보·보안 (전부 코드에 강제)

- 차량 id는 **소금 친 해시**. uuid·이메일·경로 원문은 안 나간다
- `~/.claude.json`의 `emailAddress`는 **읽지도 않는다**
- OAuth 토큰은 키체인에서 읽어 **헤더로만**. 출력·파일 어디에도 안 남는다
- `CarEvent`에 프롬프트·응답 본문 필드가 **없다** (스키마 수준)
- 화면에 이름 없음, **순위·리더보드 UI 없음**
- k-익명성 하한 10 (완화 방향으로 못 연다)

---

## 8. 벤치마크 (2026-07-31)

`docs/reference/2026-07-31-usage-visualisation-benchmark.md`.
같은 로그를 읽는 도구 20개 이상 — 전부 대시보드·메뉴바·TUI다.
**전체 화면 앰비언트는 없었다.**

가져온 것: 속도→발열 매핑(시그모이드), 유휴를 트랙에 남기기, 캐시를 금액으로.
거부한 것: 공개 순위표(`tokens.ci`), 효율 점수(`spent` — 낭비 판정 근거 없음),
프록시 래핑(실행 경로에 끼어듦), PromQL 대시보드(질의를 짜야 읽힘).

---

## 9. 파일

```
pitwall/
  src/
    types.ts            데이터 계약
    main.ts             조립 + 프레임 루프
    browser.ts          브라우저 배선 (rAF·데이터셋 선택)
    state/              clock · pace · savings · reducer · summary · ringBuffer
    track/              generateTrack · layout · spacing · trackModel
    render/             towerRenderer · modelPanel · trackRenderer · projection
                        spark · feedRenderer · radioRenderer · summaryRenderer
                        datasetPicker · legend · settingsPanel · hudRenderer
    source/             LiveSource · ReplaySource · SimulatorSource
                        claudeCodeImport · agentLogs
    radio/ director/ config/ util/
  scripts/              importClaudeCode · fetchLimits · makeDemo · liveCheck
                        bundleSingleFile · dumpEvents
  app/                  main.swift · PitwallApp.swift · LogTail.swift · build.sh
  tests/                41개 파일 547개
docs/
  superpowers/specs/    PRD · MVP 결정 · 악마의 변호인 감사
  reference/            f1-telemetry 분해 · 사용량 시각화 벤치마크
REVIEW.md               다른 작성자 의견
```

---

## 10. 잡은 버그 (재발 방지용 기록)

- **8시간 산수** — 09:00–18:00은 9시간
- **거리 50배 부풀림** — 캐시 재전송을 작업으로 셈
- **연료로 한도를 판정** — 축 붕괴
- **근무창 가정** — 오늘 작업의 61.4%가 09–18시 밖
- **시계와 이벤트가 다른 타임라인** — HUD는 오후, 데이터는 아침
- **리셋 카운트다운이 재생 시계 기준** — 3시간 49분이 15시간으로
- **되감기 시 누적 미초기화** — 15분마다 오늘 비용이 한 벌씩 증가
- **속도가 레이스 전체 평균** — IDLE인데 4.7k tok/분
- **속도 분모가 내부 시계** — $20,478/시간
- **에러 한 번이면 종일 피트** — `error_count`가 누적
- **투영 미구현** — `Math.min(LERP, 0.95)`는 항상 LERP
- **고정점에 갇힘** — 밀고 같은 앵커로 당기면 정지
- **진행률이 인덱스 기준** — 코너에서 차가 뭉침
- **스케일 시스템 없음** — 1280→3840에서 글자 크기 고정
- **선택 시 맵 밀림** — 그리드 행 비율이 바뀜
- **실시간이 데이터셋 선택을 덮어씀** — 골라도 아무 일 없음
- **트랙 시드 복원** — 며칠을 켜도 같은 코스
- **효과 없는 조작판** — 프리셋·DEMO 시계가 재생 중엔 무반응
- **CSS 삽입 실패를 반영했다고 보고** — 치환 문자열 불일치

---

## 11. 미결

### 데이터
- 세 벤더가 같은 날 돈 적이 07-19 하루뿐
- Grok 한도는 마지막 실행 시점 판독 (화면이 나이를 밝힘)
- Copilot은 세션 집계라 호출 단위가 아님
- 실기록 에러 0건 — 에러 표현은 데모로만 검증됨

### 미측정
- 8시간 연속 구동 시 힙 ≤ 50MB
- 실제 GPU 프레임률
- 3초 인지 기준선

### 결정 필요
- 연료 예산의 출처 (기본값 $60/일은 근거 없음)
- 배포용 개발자 서명
- SwiftUI 위젯 vs 상시 창

### 벤치마크 잔여
- 툴 콜 단위 귀속 — 프록시 필요, v1 범위 밖
- 프로젝트 단위 분해 — `cwd`가 PRIV와 충돌

### 서버
없다. v1은 전부 로컬.

---

## 12. 커밋 규칙

- 작성자·커미터 `정휘모 <tolaria@naver.com>`, 공동작성자 `bart.lee <bart.lee@kakao.com>`
- 푸시 전 `gh auth switch --user Hwemo-Chung`
- 원격 SHA를 `gh api`로 받아 `git push --force-with-lease=main:<SHA>`
- 저장소 `Hwemo-Chung/pitwall` (private) · 협업자 `kakao-bart-lee` (admin, 초대 대기)
